apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: demo-api-reader
  namespace: demo
rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - list
      - watch
      